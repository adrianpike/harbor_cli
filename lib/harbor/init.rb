module Harbor
  class Init
    
    def self.run
      # Look for a Procfile to determine services
      # App name
      # deploy method
      #   native:
      #     deploy_dir
      #     deploy_user
      #     servers
      #
      # Make sure the user's got ssh keys on all the servers
    end

  end
end
